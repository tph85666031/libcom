Useful API (include file,string,log,socket,thread,timer,task,sql,config,md5,crc,url,base64 ...)for cross-platform(Win Linux MacOS)



compile:
./build -h ;for help
./build    ;compile as lib
./build -u ;build unit-test tool

